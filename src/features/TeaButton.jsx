import { Container, Button } from 'reactstrap';

const TeaButton = () => {
  return (
    <Container className="d-flex">
      <Button className="my-4 mx-auto py-2 border-0">Discover a New Tea</Button>
    </Container>
  );
};

export default TeaButton;
