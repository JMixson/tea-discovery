export type Tea = {
  id: string;
  name: string;
  image?: string;
};

export type TeaDetails = {
  id: string;
  name: string;
  image: string;
  type: string;
  origin: string;
  caffeine: string;
  caffeineLevel: string;
  mainIngredients: string;
  description: string;
  colorDescription: string;
  tasteDescription: string;
  sources: string;
};
